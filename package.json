{
  "name": "geo-agent-platform",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/web",
    "packages/shared-types"
  ],
  "scripts": {
    "dev:web": "npm --workspace apps/web run dev --",
    "build:web": "npm run build --workspace apps/web",
    "lint:web": "npm run lint --workspace apps/web",
    "test:web": "npm run test --workspace apps/web",
    "dev:api": "python3 -m uvicorn api_app.main:app --reload --host 0.0.0.0 --port 8000",
    "dev:qgis-runtime": "python3 -m uvicorn qgis_runtime_app.main:app --reload --host 0.0.0.0 --port 8090",
    "test:api": "echo \"暂无后端测试用例\"",
    "test": "npm run test:api"
  },
  "dependencies": {
    "remark-gfm": "^4.0.1"
  }
}
