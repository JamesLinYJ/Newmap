// +-------------------------------------------------------------------------
//
//   地理智能平台 - 计划模式工具提示词
//
//   文件:       prompts.ts
//
//   日期:       2026年06月25日
//   作者:       OpenAI Codex
// --------------------------------------------------------------------------

// 这些提示词是从 vendor/source-code-agent-tools 的 PlanMode 工具语义
// 独立改写而来。Newmap 没有本地计划文件，计划正文通过 exit_plan_mode
// 的结构化参数进入审批和 run state。

export const ENTER_PLAN_MODE_DESCRIPTION = '进入只读探索模式，只允许查询和分析操作，不能修改数据。'

export const ENTER_PLAN_MODE_PROMPT = `在复杂执行前进入只读计划模式，让用户先审阅计划，再批准任何写入或副作用动作。

使用场景：
- 任务包含多个步骤，或存在多个合理实现路线。
- 后续可能调用写文件、导出、导入、生成报告或其他有副作用的工具。
- 需要先检查 GIS 图层、气象数据、源码文件、历史运行或 artifact，才能确定执行方案。
- 用户审批能避免返工或误操作。

进入计划模式后：
- 只能做读取、检查、查询和分析。
- 重要发现用普通 assistant 正文说明，不要塞进“思考过程”。
- todo_write 只能在计划获批后使用，不能在计划模式中使用。
- 计划准备好后，必须调用 exit_plan_mode，提交结构化 plan 供用户审批。`

export const EXIT_PLAN_MODE_DESCRIPTION = '提交结构化执行计划并请求用户批准；批准后退出只读计划模式。'

export const EXIT_PLAN_MODE_PROMPT = `只在只读计划模式已经形成完整执行计划、需要用户审阅时使用本工具。

Newmap 计划模式使用结构化参数，不写本地计划文件：
- plan.goal 是面向用户的目标。
- plan.steps 是按执行顺序排列的步骤。
- 每个步骤必须包含 id、tool、reason；已知参数应写入 args。
- 只有确实不是工具调用的人工动作，才使用 tool: "manual"。
- allowedPrompts 可以描述批准后允许执行的动作范围。

不要在以下情况使用：
- 只是简单查询，不需要实现或副作用操作。
- 只想用普通文本询问“这样可以吗”。
- 没有可审批计划，却想继续执行写入或导出动作。`
