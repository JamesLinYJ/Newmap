// +-------------------------------------------------------------------------
//
//   地理智能平台 - 媒体工具提示词
//
//   文件:       prompt.ts
//
//   日期:       2026年06月30日
//   作者:       OpenAI Codex
// --------------------------------------------------------------------------

export const TEXT_TO_SPEECH_PROMPT = `用于把用户确认的文本合成为语音音频。

使用规则：
- 只有用户明确要求语音、旁白、配音或媒体输出时才使用。
- text 参数必须是实际要朗读的内容；除非用户要求朗读，否则不要包含隐藏指令、引用说明或实现备注。
- 用户未指定音色时省略 voice，让媒体 worker 使用默认音色。
- 不要把本工具当作普通聊天回答的替代品；它会创建媒体输出，并依赖 MEDIA_WORKER_URL。`

export const DIGITAL_HUMAN_PROMPT = `用于基于已有音频和可选形象素材生成数字人说话视频。

使用规则：
- 只有已经有 text_to_speech 返回的音频路径或用户提供的音频文件时才使用。
- 这是会生成媒体文件的副作用动作，应按审批敏感工具处理。
- 不要编造本地文件路径；只能使用前序媒体工具返回的路径或用户明确提供的路径。
- 省略 avatarPath 时，由 worker 使用配置的默认形象。`
