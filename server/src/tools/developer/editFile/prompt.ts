// +-------------------------------------------------------------------------
//
//   地理智能平台 - 编辑文件工具提示词
//
//   文件:       prompt.ts
//
//   日期:       2026年06月25日
//   作者:       OpenAI Codex
// --------------------------------------------------------------------------

export const EDIT_FILE_PROMPT = `通过精确字符串替换编辑允许根目录内的 UTF-8 文本文件。

使用场景：
- 对 GIS/气象 Agent 的源码、工具 prompt、测试或样式做小范围、可审计修改。
- 保留文件整体结构，只替换一段明确文本。

规则：
- 这是有副作用的工具，必须经过审批。
- 编辑前必须先用 read_file 完整读取同一个文件，不能只读截断片段。
- old_string 必须从刚读取的内容中精确复制。
- old_string 出现多次时，只有确认每一处都应修改才设置 replace_all=true。
- 如果文件在读取后被外部修改，本工具会失败，避免覆盖用户工作。`
