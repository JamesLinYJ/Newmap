// +-------------------------------------------------------------------------
//
//   地理智能平台 - 地理编码工具提示词
//
//   文件:       prompt.ts
//
//   日期:       2026年06月30日
//   作者:       OpenAI Codex
// --------------------------------------------------------------------------

export const GEOCODE_PLACE_PROMPT = `用于把地名、POI、地址或用户给出的地点描述解析为候选坐标。

使用规则：
- 只用于地点或点位查询，不用于权威行政边界。
- 用户要求城市、区县、县域、行政区划或边界分析时，先用 list_layers 搜索平台图层；命中后用 query_layer 读取真实边界要素。
- 返回的 boundingbox 只能作为搜索上下文，不能当作行政边界、多边形分析范围或风险区划边界。
- 如果返回多个候选且目标不明确，先请求用户澄清，再继续不可逆或高影响分析。
- 下游工具接受地点引用时，优先传 place_candidate valueRef；除非下游 schema 只接受原始坐标，否则不要手抄经纬度。`
