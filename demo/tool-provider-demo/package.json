{
  "name": "tool-provider-demo",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run tests/provider.test.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24.12.2",
    "typescript": "~6.0.2",
    "vitest": "^4.0.8"
  }
}
